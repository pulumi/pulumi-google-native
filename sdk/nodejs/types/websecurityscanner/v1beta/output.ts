// *** WARNING: this file was generated by the Pulumi SDK Generator. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../../../types/input";
import * as outputs from "../../../types/output";
import * as enums from "../../../types/enums";
import * as utilities from "../../../utilities";

/**
 * Scan authentication configuration.
 */
export interface AuthenticationResponse {
    /**
     * Authentication using a custom account.
     */
    customAccount: outputs.websecurityscanner.v1beta.CustomAccountResponse;
    /**
     * Authentication using a Google account.
     */
    googleAccount: outputs.websecurityscanner.v1beta.GoogleAccountResponse;
    /**
     * Authentication using Identity-Aware-Proxy (IAP).
     */
    iapCredential: outputs.websecurityscanner.v1beta.IapCredentialResponse;
}

/**
 * Describes authentication configuration that uses a custom account.
 */
export interface CustomAccountResponse {
    /**
     * The login form URL of the website.
     */
    loginUrl: string;
    /**
     * Input only. The password of the custom account. The credential is stored encrypted and not returned in any response nor included in audit logs.
     */
    password: string;
    /**
     * The user name of the custom account.
     */
    username: string;
}

/**
 * Describes authentication configuration that uses a Google account.
 */
export interface GoogleAccountResponse {
    /**
     * Input only. The password of the Google account. The credential is stored encrypted and not returned in any response nor included in audit logs.
     */
    password: string;
    /**
     * The user name of the Google account.
     */
    username: string;
}

/**
 * Describes authentication configuration for Identity-Aware-Proxy (IAP).
 */
export interface IapCredentialResponse {
    /**
     * Authentication configuration when Web-Security-Scanner service account is added in Identity-Aware-Proxy (IAP) access policies.
     */
    iapTestServiceAccountInfo: outputs.websecurityscanner.v1beta.IapTestServiceAccountInfoResponse;
}

/**
 * Describes authentication configuration when Web-Security-Scanner service account is added in Identity-Aware-Proxy (IAP) access policies.
 */
export interface IapTestServiceAccountInfoResponse {
    /**
     * Describes OAuth2 Client ID of resources protected by Identity-Aware-Proxy(IAP).
     */
    targetAudienceClientId: string;
}

/**
 * Defines a custom error message used by CreateScanConfig and UpdateScanConfig APIs when scan configuration validation fails. It is also reported as part of a ScanRunErrorTrace message if scan validation fails due to a scan configuration error.
 */
export interface ScanConfigErrorResponse {
    /**
     * Indicates the reason code for a configuration failure.
     */
    code: string;
    /**
     * Indicates the full name of the ScanConfig field that triggers this error, for example "scan_config.max_qps". This field is provided for troubleshooting purposes only and its actual value can change in the future.
     */
    fieldName: string;
}

/**
 * Output only. Defines an error trace message for a ScanRun.
 */
export interface ScanRunErrorTraceResponse {
    /**
     * Indicates the error reason code.
     */
    code: string;
    /**
     * If the scan encounters TOO_MANY_HTTP_ERRORS, this field indicates the most common HTTP error code, if such is available. For example, if this code is 404, the scan has encountered too many NOT_FOUND responses.
     */
    mostCommonHttpErrorCode: number;
    /**
     * If the scan encounters SCAN_CONFIG_ISSUE error, this field has the error message encountered during scan configuration validation that is performed before each scan run.
     */
    scanConfigError: outputs.websecurityscanner.v1beta.ScanConfigErrorResponse;
}

/**
 * A ScanRun is a output-only resource representing an actual run of the scan. Next id: 12
 */
export interface ScanRunResponse {
    /**
     * The time at which the ScanRun reached termination state - that the ScanRun is either finished or stopped by user.
     */
    endTime: string;
    /**
     * If result_state is an ERROR, this field provides the primary reason for scan's termination and more details, if such are available.
     */
    errorTrace: outputs.websecurityscanner.v1beta.ScanRunErrorTraceResponse;
    /**
     * The execution state of the ScanRun.
     */
    executionState: string;
    /**
     * Whether the scan run has found any vulnerabilities.
     */
    hasVulnerabilities: boolean;
    /**
     * The resource name of the ScanRun. The name follows the format of 'projects/{projectId}/scanConfigs/{scanConfigId}/scanRuns/{scanRunId}'. The ScanRun IDs are generated by the system.
     */
    name: string;
    /**
     * The percentage of total completion ranging from 0 to 100. If the scan is in queue, the value is 0. If the scan is running, the value ranges from 0 to 100. If the scan is finished, the value is 100.
     */
    progressPercent: number;
    /**
     * The result state of the ScanRun. This field is only available after the execution state reaches "FINISHED".
     */
    resultState: string;
    /**
     * The time at which the ScanRun started.
     */
    startTime: string;
    /**
     * The number of URLs crawled during this ScanRun. If the scan is in progress, the value represents the number of URLs crawled up to now.
     */
    urlsCrawledCount: string;
    /**
     * The number of URLs tested during this ScanRun. If the scan is in progress, the value represents the number of URLs tested up to now. The number of URLs tested is usually larger than the number URLS crawled because typically a crawled URL is tested with multiple test payloads.
     */
    urlsTestedCount: string;
    /**
     * A list of warnings, if such are encountered during this scan run.
     */
    warningTraces: outputs.websecurityscanner.v1beta.ScanRunWarningTraceResponse[];
}

/**
 * Output only. Defines a warning trace message for ScanRun. Warning traces provide customers with useful information that helps make the scanning process more effective.
 */
export interface ScanRunWarningTraceResponse {
    /**
     * Indicates the warning code.
     */
    code: string;
}

/**
 * Scan schedule configuration.
 */
export interface ScheduleResponse {
    /**
     * The duration of time between executions in days.
     */
    intervalDurationDays: number;
    /**
     * A timestamp indicates when the next run will be scheduled. The value is refreshed by the server after each run. If unspecified, it will default to current server time, which means the scan will be scheduled to start immediately.
     */
    scheduleTime: string;
}

